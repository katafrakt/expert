defmodule TokenMetadataCompilation.MixProject do
  use Mix.Project

  def project do
    [
      app: :token_metadata_compilation,
      version: "0.1.0",
      elixir: "~> 1.15",
      deps: []
    ]
  end
end
